{
  "name": "javascript-prepare",
  "version": "1.0.0",
  "description": "",
  "source": "language-management/src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf dist && rm -rf .parcel-cache && parcel ./src/*.html --no-cache --no-source-maps --dist-dir dist",
    "build": "rm -rf dist && rm -rf .parcel-cache && parcel build ./src/*.html --no-cache --no-source-maps --dist-dir dist"
  },
  "author": "Duc Le",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.3",
    "process": "^0.11.10",
    "util": "^0.12.5"
  },
  "dependencies": {
    "json-server": "^0.17.3",
    "uuidv4": "^6.2.13"
  }
}
